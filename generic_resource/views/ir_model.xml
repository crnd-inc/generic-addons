<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_model_form" model="ir.ui.view">
        <field name="model">ir.model</field>
        <field name="inherit_id" ref="base.view_model_form"/>
        <field name="arch" type="xml">
            <field name="transient" position="after">
                <field groups="base.group_no_one" name="is_generic_resource" readonly="state != 'manual'"/>
            </field>
        </field>
    </record>

    <record id="view_model_search" model="ir.ui.view">
        <field name="model">ir.model</field>
        <field name="inherit_id" ref="base.view_model_search"/>
        <field name="arch" type="xml">
            <field name="model" position="after">
                <filter domain="[('is_generic_resource', '=', True)]" name="filter_is_generic_resource" string="Generic Resource"/>
            </field>
        </field>
    </record>
</odoo>

