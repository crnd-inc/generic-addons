<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action Rule Form View -->
    <record id="view_base_automation_form" model="ir.ui.view">
        <field name="name">base.automation.form.tags</field>
        <field name="model">base.automation</field>
        <field name="inherit_id" ref="base_automation.view_base_automation_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_title')]" position="after">
                <field context="{'default_model': model_name}" name="act_add_tag_ids" options="{'color_field': 'color'}" widget="many2many_tags"/>
                <field context="{'default_model': model_name}" name="act_remove_tag_ids" options="{'color_field': 'color'}" widget="many2many_tags"/>
            </xpath>
        </field>
    </record>
</odoo>
